# Angola B2B WordPress主题开发规则

## 🎯 项目概述
**项目名称**: Unibro B2B产品展示网站  
**项目类型**: WordPress自定义主题（B2B外贸展示站）  
**主题名称**: angola-b2b  
**版本**: 1.0.0  
**目标市场**: 安哥拉市场，中国产品出口  
**核心业务**: 建筑材料、农业机械、工业设备等B2B产品展示与询盘

---

## 📁 项目目录结构

### 核心路径
```
F:\011 Projects\UnibroWeb\Unirbro\
├── wp-content/themes/angola-b2b/          # 主题开发目录（Git跟踪）⭐
│   ├── assets/                            # 静态资源
│   │   ├── css/                          # 样式文件（模块化CSS）
│   │   ├── js/                           # JavaScript文件
│   │   └── images/                       # 主题图片资源
│   ├── inc/                              # PHP功能模块
│   ├── template-parts/                   # 模板片段
│   │   ├── homepage/                     # 首页模块
│   │   ├── global/                       # 全局组件
│   │   └── product/                      # 产品相关
│   ├── header.php                        # MSC风格Header
│   ├── footer.php                        # Footer（含MSC风格搜索框）
│   ├── front-page.php                    # 首页模板
│   ├── functions.php                     # 主题功能入口
│   └── style.css                         # 主题信息头
│
├── local-site/angola-b2b/                # Local开发环境（不Git跟踪）
│   └── app/public/wp-content/themes/angola-b2b/  # 符号链接→开发目录
│
└── PICS for TEST/                        # 测试图片素材
```

### 关键说明
- **开发目录**: `wp-content/themes/angola-b2b/` - 所有代码修改在此进行
- **测试站点**: `http://angola-b2b.local/` - Local by Flywheel
- **符号链接**: Local站点主题自动同步开发目录修改
- **Git操作**: 始终在主题开发目录执行

---

## 🛠️ 技术栈

### WordPress环境
- **WordPress版本**: 6.x
- **PHP版本**: 7.4+
- **开发工具**: Local by Flywheel
- **版本控制**: Git

### 核心技术
1. **自定义文章类型 (CPT)**:
   - `product` (产品) - 主要内容类型
   
2. **自定义分类法 (Taxonomy)**:
   - `product_category` (产品分类) - 层级分类系统

3. **ACF Free**:
   - ⚠️ 使用免费版，无Options Page功能
   - 解决方案：首页设置存储在WordPress页面（ID: 45）
   - 用法：`get_field('field_name', 45)` 而非 `get_field('field_name', 'option')`

4. **多语言系统**:
   - 自定义Cookie-based多语言（无插件依赖）
   - 支持语言：英语(en)、葡萄牙语(pt)、简体中文(zh)、繁体中文(zh_tw)
   - 核心文件：`inc/custom-multilingual.php`

5. **前端库**:
   - Swiper.js v11+ (轮播)
   - 原生JavaScript (无jQuery)

---

## 🎨 设计风格与UI组件

### Header设计（MSC风格）
**特点**: 透明悬浮 → 滚动后白色背景

**结构**:
```
[Logo]  [主导航]  [🔍 Search]  [📞 Contact]  [🌐 EN]
         (居中)      (垂直布局：图标在上，文字在下)
```

**状态切换**:
- 页面顶部：透明背景，白色文字/图标
- 滚动后（>50px）：白色背景（box-shadow），黑色文字/图标

**交互组件**:
1. **Search按钮**: 点击显示MSC风格搜索框（白色圆角，半透明遮罩，点击外部隐藏）
2. **Contact按钮**: 下拉显示邮箱和电话
3. **Language按钮**: 下拉显示4种语言（当前语言黄色高亮#FDB913）

**关键文件**:
- `header.php` - HTML结构
- `assets/css/layout.css` - Header样式
- `assets/js/main.js` - 滚动效果
- `assets/js/header-search.js` - 搜索框交互

### Hero Section
**特点**: 全宽背景图片，Header悬浮其上

**实现**:
- `margin-top: -80px` (桌面) / `-70px` (移动)
- `padding-top: 80px` / `70px` (补偿内容位置)
- 响应式padding：16px (移动) / 24px (平板) / 32px (桌面)
- 背景图片带遮罩层（rgba(0,0,0,0.4)）

**关键文件**:
- `template-parts/global/hero-section.php`
- `assets/css/hero-section.css`

### 首页模块（当前启用）
1. **Hero区域** - 全屏背景图 + 标题 + Quick Actions
2. **产品大分类展示** - 4个大卡片（建筑工程、建筑材料、农机农具、工业设备）
3. **新闻轮播** - Discover the Latest News

**暂时隐藏（代码保留）**:
- 服务展示轮播
- 网络展示轮播
- 行业展示轮播
- 统计数字展示
- 客户公告列表
- 核心优势区域
- CTA联系我们区域

---

## 📋 ACF字段配置

### 首页设置（Page ID: 45）
**简化版结构** - 无Tab分类，所有字段在一个页面

#### Hero区域设置
- `hero_background_image` (图片) - Hero背景图（建议1920x800px）
- `hero_title` (文本) - Hero主标题
- `hero_subtitle` (文本域) - Hero副标题
- `hero_cta_primary_text` (文本) - 主按钮文字
- `hero_cta_primary_url` (URL) - 主按钮链接
- `hero_cta_secondary_text` (文本) - 次按钮文字
- `hero_cta_secondary_url` (URL) - 次按钮链接

#### 联系信息
- `contact_email` (邮箱) - 联系邮箱
- `contact_phone` (文本) - 联系电话

#### 社交媒体链接
- `social_facebook` (URL) - Facebook链接
- `social_facebook_show` (真/假) - 是否显示Facebook
- `social_twitter` (URL) - Twitter链接
- `social_twitter_show` (真/假) - 是否显示Twitter
- `social_linkedin` (URL) - LinkedIn链接
- `social_linkedin_show` (真/假) - 是否显示LinkedIn
- `social_whatsapp` (文本) - WhatsApp号码（国际格式）
- `social_whatsapp_show` (真/假) - 是否显示WhatsApp

#### 内容页面（多语言）
**关于我们**:
- `footer_about_us_content` (WYSIWYG) - 英文内容
- `footer_about_us_content_pt` (WYSIWYG) - 葡萄牙语内容
- `footer_about_us_content_zh` (WYSIWYG) - 简体中文内容
- `footer_about_us_content_zh_tw` (WYSIWYG) - 繁体中文内容

**我们的服务**:
- `footer_services_content` (WYSIWYG) - 英文内容
- `footer_services_content_pt` (WYSIWYG) - 葡萄牙语内容
- `footer_services_content_zh` (WYSIWYG) - 简体中文内容
- `footer_services_content_zh_tw` (WYSIWYG) - 繁体中文内容

**招聘**:
- `footer_careers_content` (WYSIWYG) - 英文内容
- `footer_careers_content_pt` (WYSIWYG) - 葡萄牙语内容
- `footer_careers_content_zh` (WYSIWYG) - 简体中文内容
- `footer_careers_content_zh_tw` (WYSIWYG) - 繁体中文内容

**页面模板**:
- `page-about.php` - 关于我们页面模板
- `page-services.php` - 我们的服务页面模板
- `page-careers.php` - 招聘页面模板

### 产品字段（Product CPT）
**基本信息**:
- WordPress特色图片 - 产品主图（优先使用）
- 标题、内容、分类 - WordPress原生

**ACF自定义字段**:
- `product_image_1` ~ `product_image_5` (图片) - 额外产品图
- `product_specs_*` (文本) - 产品规格参数
- `product_in_stock` (真/假) - 是否有库存
- `product_featured` (真/假) - 是否精选
- `product_stock_quantity` (数字) - 库存数量

---

## 📝 开发规范

### 代码风格
1. **WordPress编码标准**: 遵循WordPress Coding Standards
2. **安全性**: 
   - 所有输出使用 `esc_html()`, `esc_attr()`, `esc_url()`
   - 所有输入使用 `sanitize_text_field()` 等函数
3. **可访问性**: 遵循WCAG 2.1标准
4. **性能优化**: 
   - CSS/JS文件模块化
   - 合理使用缓存
   - 图片懒加载

### 文件组织
**CSS结构** (模块化):
```
assets/css/
├── variables.css         # CSS变量（颜色、间距、字体）
├── reset.css            # CSS重置
├── base.css             # 基础样式
├── layout.css           # 布局样式（Header、Footer）
├── components.css       # 可复用组件
├── hero-section.css     # Hero区域
├── homepage.css         # 首页特定样式
└── responsive.css       # 响应式适配
```

**JavaScript结构**:
```
assets/js/
├── main.js              # 核心功能（Header滚动、初始化）
├── header-search.js     # MSC风格搜索框
├── language-switcher.js # 语言切换
├── mobile-menu.js       # 移动菜单
└── utils.js             # 工具函数
```

**PHP模块** (`inc/`):
```
inc/
├── custom-multilingual.php    # 自定义多语言系统
├── custom-post-types.php      # 注册CPT
├── custom-taxonomies.php      # 注册分类法
├── acf-fields.php             # ACF字段定义
├── theme-setup.php            # 主题初始化
├── enqueue-scripts.php        # 资源加载
├── helpers.php                # 辅助函数
└── admin-*.php                # 后台定制
```

### 命名规范
- **函数**: `angola_b2b_function_name()`
- **变量**: `$variable_name` (snake_case)
- **CSS类**: `.class-name` (kebab-case)
- **常量**: `ANGOLA_B2B_CONSTANT_NAME`

---

## 🌐 多语言系统

### 实现方式
基于Cookie的自定义方案，无需插件

### 语言配置
```php
支持语言:
- en (English) - 默认语言
- pt (Português) 
- zh (简体中文)
- zh_tw (繁體中文)
```

### 语言切换逻辑
1. 检查URL参数 `?lang=xx`
2. 检查Cookie `angola_b2b_lang`
3. 检查浏览器语言偏好
4. 默认使用英语

### 使用方法
```php
// 获取当前语言
$current_lang = angola_b2b_get_current_language();

// 获取翻译
$translated = angola_b2b_translate_field($field_name, $post_id);

// 语言切换器
angola_b2b_display_language_switcher();
```

### 多语言字段
ACF字段自动创建4个语言版本：
- `field_name` (英语)
- `field_name_pt` (葡萄牙语)
- `field_name_zh` (简体中文)
- `field_name_zh_tw` (繁体中文)

---

## 🎯 核心功能模块

### 1. Header功能
**文件**: `header.php`, `assets/css/layout.css`, `assets/js/main.js`

**功能点**:
- ✅ 透明悬浮效果（页面顶部）
- ✅ 滚动后白色背景（>50px）
- ✅ Logo居左，导航居中，操作按钮居右
- ✅ MSC风格搜索框（点击显示/隐藏）
- ✅ Contact下拉菜单（邮箱+电话）
- ✅ Language下拉菜单（4种语言，当前语言黄色高亮）
- ✅ 移动端响应式（汉堡菜单）

### 2. Hero Section
**文件**: `template-parts/global/hero-section.php`, `assets/css/hero-section.css`

**功能点**:
- ✅ 全宽背景图片
- ✅ Header悬浮其上
- ✅ 响应式padding（与其他section一致）
- ✅ 深色遮罩（rgba(0,0,0,0.4)）
- ✅ 标题+副标题+双按钮

### 3. 产品分类展示
**文件**: `template-parts/homepage/product-categories-showcase.php`

**功能点**:
- ✅ 4个大分类卡片
- ✅ Hover效果（图片缩放）
- ✅ 响应式布局（桌面4列、平板2列、移动1列）

### 4. 新闻轮播
**文件**: `template-parts/homepage/news-carousel.php`

**功能点**:
- ✅ Swiper轮播
- ✅ 响应式卡片数量
- ✅ 导航按钮+分页器

---

## 🔧 常用操作

### 创建WordPress页面（推荐方法）⭐
**问题**: 需要创建新的WordPress页面时，不要使用命令行或临时脚本

**最佳实践**: 使用WordPress后台管理界面创建页面
1. 创建PHP文件定义页面创建函数（放在 `inc/` 目录）
2. 添加后台管理菜单和界面
3. 在 `functions.php` 中引入该文件
4. 管理员在后台点击按钮即可创建

**示例代码结构**:
```php
// inc/create-default-pages.php
function theme_create_pages() {
    $pages = array(
        array(
            'post_title' => 'Page Title',
            'post_name' => 'page-slug',
            'post_status' => 'publish',
            'post_type' => 'page',
            'page_template' => 'page-template.php'
        ),
    );
    
    foreach ($pages as $page) {
        $existing = get_page_by_path($page['post_name']);
        if (!$existing) {
            $page_id = wp_insert_post($page);
            update_post_meta($page_id, '_wp_page_template', $page['page_template']);
        }
    }
}

// 添加后台管理菜单
add_action('admin_menu', function() {
    add_submenu_page('themes.php', '创建页面', '创建页面', 'manage_options', 'create-pages', 'render_page');
});

// 渲染管理界面
function render_page() {
    if (isset($_POST['create']) && check_admin_referer('create_pages')) {
        theme_create_pages();
        echo '<div class="notice notice-success"><p>创建成功！</p></div>';
    }
    // 显示表单和页面状态...
}
```

**优点**:
- ✅ 可视化界面，易于操作
- ✅ 显示页面状态（已创建/未创建）
- ✅ 可重复执行，自动跳过已存在的页面
- ✅ 无需命令行，避免PowerShell路径问题
- ✅ 符合WordPress最佳实践

**已实现**: `inc/create-default-pages.php` - About/Services/Careers页面创建

---

### Git工作流
```bash
# 切换到主题目录
cd "F:\011 Projects\UnibroWeb\Unirbro\wp-content\themes\angola-b2b"

# 查看状态
git status

# 添加修改
git add .

# 提交（使用英文commit message）
git commit -m "Descriptive commit message"

# 查看历史
git log --online -5
```

### 常用ACF函数
```php
// 获取首页设置字段
$hero_title = get_field('hero_title', 45);

// 获取产品字段
$is_featured = get_field('product_featured', $post_id);

// 获取翻译字段
$title = angola_b2b_translate_field('hero_title', 45);
```

### 常用WordPress函数
```php
// 获取特色图片URL
$image_url = get_the_post_thumbnail_url($post_id, 'large');

// 产品查询
$products = new WP_Query(array(
    'post_type' => 'product',
    'posts_per_page' => 20,
    'meta_key' => 'product_featured',
    'meta_value' => '1'
));

// 分类查询
$categories = get_terms(array(
    'taxonomy' => 'product_category',
    'hide_empty' => false
));
```

---

## ⚠️ 已知问题与解决方案

### 1. ACF Options Page不可用
**问题**: ACF免费版无Options Page  
**解决**: 使用WordPress页面（ID: 45）存储首页设置  
**用法**: `get_field('field_name', 45)`

### 2. 语言切换URL结构
**问题**: 使用"子域名"方式会导致403错误  
**解决**: Polylang设置使用"目录"方式（/en/, /pt/）

### 3. 产品图片优先级
**问题**: ACF图片字段可能为空  
**解决**: 优先使用`get_the_post_thumbnail_url()`，ACF图片作为额外图库

### 4. PowerShell命令限制
**问题**: PowerShell不支持`&&`连接命令  
**解决**: 分步执行命令，使用`;`分隔或多次执行

---

## 🚀 开发工作流

### 日常开发
1. 在`wp-content/themes/angola-b2b/`修改代码
2. 浏览器访问`http://angola-b2b.local/`测试
3. 符号链接自动同步，刷新浏览器即可看到效果
4. 完成后Git提交

### 新功能开发
1. 在`template-parts/`创建模板片段
2. 在`assets/css/`添加对应样式
3. 在`assets/js/`添加交互脚本
4. 在`inc/enqueue-scripts.php`注册新文件
5. 在模板中引用：`get_template_part('template-parts/...')`

### 调试技巧
1. 启用WordPress调试：`define('WP_DEBUG', true);`
2. 查看错误日志：`wp-content/debug.log`
3. 浏览器开发者工具：检查CSS/JS
4. ACF字段调试：`var_dump(get_field('field_name', 45));`

---

## 📞 沟通规范

### AI助手工作方式
1. **禁止展示代码**: 用户不懂代码，用自然语言解释即可
2. **禁止创建文档**: 所有沟通在对话中完成，不创建.md文件
3. **直接执行**: 需要修改时直接编辑文件，无需询问
4. **质量保证**: 完成一批文件后立即自检，确保代码质量

### 用户偏好
- 喜欢看到实际效果，不喜欢看代码
- 需要视觉化的设计参考（如MSC.com）
- 希望功能完整、易用、美观
- 重视后台管理的简洁性

---

## 📊 项目当前状态

### 已完成功能 ✅
1. ✅ MSC风格Header（透明悬浮、滚动切换）
2. ✅ MSC风格搜索框（点击显示/隐藏）
3. ✅ 语言切换器（4种语言，黄色高亮）
4. ✅ Hero Section（全宽显示，响应式padding）
5. ✅ 产品分类展示（4个大卡片）
6. ✅ 新闻轮播（Swiper）
7. ✅ 自定义多语言系统
8. ✅ 后台管理简化（无Tab分类，单页设置）
9. ✅ 社交媒体链接设置（Facebook、Twitter、LinkedIn、WhatsApp）
10. ✅ 内容页面系统（About、Services、Careers）
    - 多语言WYSIWYG编辑器（4种语言）
    - 自动语言切换和内容回退
    - 后台管理界面创建页面
    - 响应式内容页面样式

### 待优化功能 🔄
1. 🔄 图片灯箱功能（PhotoSwipeLightbox）
2. 🔄 移动端菜单优化
3. 🔄 产品筛选功能
4. 🔄 询盘系统完善

### 暂时隐藏的模块 💤
- 服务展示轮播
- 网络展示轮播
- 行业展示轮播
- 统计数字展示
- 客户公告列表
- 核心优势区域
- CTA联系我们区域

> 这些模块代码已完成并保留，需要时可快速启用

---

## 🎓 WordPress知识要点

### 模板层级
```
front-page.php (首页) → index.php (后备)
single-product.php (单个产品) → single.php → index.php
archive-product.php (产品列表) → archive.php → index.php
taxonomy-product_category.php (分类页) → taxonomy.php → archive.php
```

### 核心钩子
```php
// 初始化
add_action('after_setup_theme', 'function_name');

// 资源加载
add_action('wp_enqueue_scripts', 'function_name');

// 修改查询
add_action('pre_get_posts', 'function_name');

// 过滤内容
add_filter('the_content', 'function_name');
```

### ACF钩子
```php
// 初始化
add_action('acf/init', 'function_name');

// 保存后
add_action('acf/save_post', 'function_name');
```

---

## 📚 参考资源

### 设计参考
- **MSC.com** - Header、搜索框、整体风格
- **现代B2B网站** - 产品展示、卡片设计

### 技术文档
- WordPress Codex: https://codex.wordpress.org/
- ACF Documentation: https://www.advancedcustomfields.com/resources/
- Swiper.js: https://swiperjs.com/

---

## 🎯 下一步规划

### 短期目标
1. 完善产品详情页设计
2. 优化移动端体验
3. 添加产品筛选功能
4. 完善询盘系统

### 中期目标
1. 性能优化（缓存、CDN）
2. SEO优化（Schema.org）
3. 添加更多内容模块
4. 多语言内容完善

### 长期目标
1. 集成在线支付（如需要）
2. 会员系统（如需要）
3. 数据分析与报表
4. 移动App（如需要）

---

**最后更新**: 2025-11-09  
**当前版本**: 1.0.0  
**项目状态**: 开发中（核心功能已完成）

---

## 🔑 快速命令参考

```bash
# Git基本操作
cd "F:\011 Projects\UnibroWeb\Unirbro\wp-content\themes\angola-b2b"
git status
git add .
git commit -m "Message"
git log --oneline -5

# 常用文件路径
主题目录: wp-content/themes/angola-b2b/
测试站点: http://angola-b2b.local/
首页设置: http://angola-b2b.local/wp-admin/post.php?post=45&action=edit
产品管理: http://angola-b2b.local/wp-admin/edit.php?post_type=product
```
